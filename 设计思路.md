1. 实现一个最小实例，我已经创建了一个用于本项目的miniconda虚拟环境“107”，采用napcat作为客户端，协议使用websocket,写一个程序开一个websocket服务器，监听3001端口。功能只需要简单的把qq的用户消息转发给deepseek, 然后获取回答并回复即可，你需要把环境变量单独分出来写成配置文件，然后加一个机制，只有特定用户才回答，用qq号区分，配置哪些qq号同样也用配置文件写好（格式json或者普通文本均可）。

2.